# Python 3.10.11
#
# Core scraping and data handling
# Versions reflect the tested environment (pip freeze 2026-03-22).
requests==2.32.5
pandas==2.3.3
beautifulsoup4==4.14.3
lxml==6.0.2
playwright==1.58.0
numpy==2.2.6
pdfplumber
openpyxl
yfinance

# LLM / NLP analysis
# These packages depend heavily on hardware (CPU vs GPU) and CUDA version.
# Install separately and pin to the version that matches your hardware.
transformers
torch
tqdm
